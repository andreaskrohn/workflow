'use client'
import React from 'react'

interface TagManagerProps {
  taskId: string
}

// Stub — Agent E owns the internals.
export default function TagManager({ taskId: _taskId }: TagManagerProps) {
  return <div className="text-slate-500 text-sm">Tags coming soon.</div>
}
